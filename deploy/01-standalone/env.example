# 复制为 env.sh 后填写，再 source 使用（勿提交含真实地址的 env.sh 若含敏感信息）
# cp env.example env.sh

# GitLab 服务器
GITLAB_HOST=gitlab.example.com
GITLAB_EXTERNAL_URL=http://gitlab.example.com

# Jenkins 服务器
JENKINS_HOST=jenkins.example.com
JENKINS_HTTP_PORT=8080

# 时区
TZ=Asia/Shanghai
